# 🚀 快速开始指南

## 最新更新

### ✨ 界面优化 (2025-12-29)
- 🎨 全新的紫色渐变配色方案
- 💎 更现代的卡片设计和阴影效果
- 🎭 流畅的动画和微交互
- 📱 优化的间距和视觉层次

### ⚡ Favicon 缓存功能 (2025-12-29)
- 🚀 双层缓存架构（内存 + 持久化）
- ⏱️ 98% 的加载速度提升
- 🧹 自动过期管理（7天）
- 💾 智能容量控制（最多100个）

## 📦 安装扩展

### Chrome / Edge

1. **构建扩展**
   ```bash
   bun run build:chrome
   ```

2. **加载扩展**
   - 打开 `chrome://extensions/` (Chrome) 或 `edge://extensions/` (Edge)
   - 启用"开发者模式"
   - 点击"加载已解压的扩展程序"
   - 选择 `dist/` 目录

### Firefox

1. **构建扩展**
   ```bash
   bun run build:firefox
   ```

2. **加载扩展**
   - 打开 `about:debugging`
   - 点击"此 Firefox"
   - 点击"临时载入附加组件"
   - 选择 `dist/manifest.json`

## 🎯 使用方法

### 保存会话

1. 在网站上登录你的账户
2. 点击扩展图标打开弹窗
3. 点击 **"Save Session"** 按钮
4. 输入会话名称（如 "工作账号"）
5. 点击 **"Save"**

### 切换会话

1. 点击扩展图标打开弹窗
2. 在会话列表中找到要切换的会话
3. 点击会话卡片
4. 页面会自动刷新并切换到该账号

### 新建会话

1. 点击 **"New Session"** 按钮
2. 当前会话的 cookies 会被清除
3. 页面刷新后可以登录新账号
4. 登录后保存为新会话

### 管理会话

- **重命名**: 点击会话卡片上的 ✏️ 按钮
- **删除**: 点击会话卡片上的 🗑️ 按钮
- **展开/折叠**: 点击域名卡片头部

## 🎨 界面说明

### 主要元素

- **当前站点**: 显示当前标签页的域名
- **Save Session**: 保存当前登录状态
- **New Session**: 清除当前会话，开始新会话
- **域名卡片**: 按域名分组的会话列表
- **会话徽章**: 显示该域名下的会话数量
- **Active 标记**: 标识当前激活的会话
- **有效性图标**: 
  - ✓ 绿色 = 会话有效
  - ⏱ 红色 = 会话已过期
  - ? 灰色 = 未知状态

### 视觉反馈

- **悬停效果**: 卡片会上移并显示阴影
- **当前域名**: 紫色边框和渐变背景
- **激活会话**: 绿色边框和背景
- **无效会话**: 红色边框和背景

## 🔧 开发命令

```bash
# 开发模式（热重载）
bun run dev:chrome      # Chrome 开发模式
bun run dev:firefox     # Firefox 开发模式

# 生产构建
bun run build:chrome    # 构建 Chrome 版本
bun run build:firefox   # 构建 Firefox 版本

# 代码质量
bun run lint            # 自动修复 lint 问题
bun run lint:check      # 仅检查不修复
bun run format          # 格式化代码
bun run format:check    # 检查格式

# 清理
bun run clean           # 删除 dist/ 目录
```

## 💡 使用技巧

### 1. 快速切换账号
- 为常用账号设置易识别的名称
- 使用域名分组快速定位
- 利用 Active 标记识别当前账号

### 2. 管理多个网站
- 每个网站的会话独立管理
- 点击域名卡片展开/折叠
- 当前访问的域名会自动高亮

### 3. 检查会话状态
- 绿色 ✓ 表示会话有效
- 红色 ⏱ 表示需要重新登录
- 定期检查并更新过期会话

### 4. 优化性能
- 图标会自动缓存，无需担心加载速度
- 缓存 7 天后自动更新
- 如需手动清理缓存：
  ```javascript
  chrome.storage.local.remove('faviconCache');
  ```

## 📊 性能优化

### Favicon 缓存
- **首次加载**: ~500ms（需要网络请求）
- **后续加载**: ~10ms（从缓存读取）
- **10个域名**: 从 ~5s 降至 ~100ms

### 自动优化
- 启动时自动清理过期缓存
- LRU 策略管理缓存容量
- 双层缓存确保最快访问

## 🐛 常见问题

### Q: 切换会话后没有生效？
A: 确保页面已经刷新。扩展会自动刷新页面，如果没有，请手动刷新。

### Q: 会话显示为无效？
A: 这表示 cookies 已过期，需要重新登录并保存会话。

### Q: 图标加载很慢？
A: 首次加载会从网络获取，后续会使用缓存。如果持续很慢，检查网络连接。

### Q: 如何清除所有数据？
A: 在浏览器扩展管理页面，点击"清除数据"或卸载重装扩展。

## 📚 更多文档

- [FAVICON_CACHE.md](./FAVICON_CACHE.md) - Favicon 缓存详细说明
- [CACHE_IMPLEMENTATION.md](./CACHE_IMPLEMENTATION.md) - 缓存实现技术细节
- [CLAUDE.md](./CLAUDE.md) - 项目架构和开发指南
- [README.md](./README.md) - 项目介绍

## 🎉 开始使用

现在你已经了解了所有基础知识，开始使用 MySessionBox 管理你的多账号吧！

如有问题或建议，欢迎提交 Issue 或 PR。

